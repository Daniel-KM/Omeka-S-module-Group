<?php
/**
 * @var \Laminas\View\Renderer\PhpRenderer $this
 * @var \Group\Api\Representation\GroupRepresentation $group
 * @var \Omeka\Api\Representation\AbstractEntityRepresentation $resource
 */

$plugins = $this->getHelperPluginManager();
$url = $plugins->get('url');
$escape = $plugins->get('escapeHtml');
$translate = $plugins->get('translate');

$this->htmlElement('body')->appendAttribute('class', 'groups show');
?>

<?= $this->pageTitle($resource->name(), 1, $translate('Groups')) ?>

<div id="page-actions">
</div>
<div class="breadcrumbs">
    <a href="<?= $url('admin/group') ?>" class="o-icon-left"><?= $translate('Back to group list') ?></a>
</div>

<?php $this->trigger('view.show.before') ?>
<div class="property">
    <h4><?= $escape($translate('Name')) ?></h4>
    <div class="value"><?= $escape($resource->name()) ?></div>
</div>
<div class="property">
    <h4><?= $escape($translate('Comment')) ?></h4>
    <div class="value"><?= $escape($resource->comment()) ?></div>
</div>
<div class="property">
    <h4><?= $escape($translate('Internal id')) ?></h4>
    <div class="value"><?= $escape($resource->id()) ?></div>
</div>
<?php $this->trigger('view.show.after') ?>

<p>
    <em><?= $escape($translate('Groups are editable on the browse page.')) ?></em>
</p>
